[project]
name = "wows-minimap-renderer"
version = "0.3.0"
requires-python = ">=3.12"
license = { file = "LICENSE" }
dependencies = [
    "wows-replay-parser>=0.1.3",
    "pycairo>=1.29.0",
    "cairosvg>=2.9.0",
    "discord.py>=2.7.1",
    "click>=8.3.3",
    "rich>=15.0.0",
    "python-dotenv>=1.2.2",
    "av>=17.0.1",
    "numpy>=2.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
    "mypy>=1.10",
    "pytest>=9.0.3",
    "Pillow>=12.2.0",
]

[project.scripts]
wows-render = "renderer.cli:main"
wows-bot = "bot.main:main"

[tool.hatch.build.targets.wheel]
packages = ["renderer", "bot"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
# Temporary git pin for the dual-merge dedup fix (clock alignment +
# server-broadcast event dedup) until it's published to PyPI. Public repo,
# so the Docker build fetches it over https with no extra auth.
wows-replay-parser = { git = "https://github.com/toalba/wows-replay-parser.git", rev = "ddb22241f0e410c2ddda020e45a245971a7134d2" }

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "SIM", "TCH"]
ignore = [
    "TC001",  # typing-only first-party import
    "TC002",  # typing-only third-party import (cairo is used at runtime)
    "TC003",  # typing-only stdlib import
    "N806",   # non-lowercase local
    "SIM102", # collapsible if
    "SIM103", # needless bool
    "SIM105", # contextlib.suppress over try/except/pass
    "SIM108", # ternary over if/else
    "SIM114", # combine if branches with or
    "SIM202", # negate != op
    "B007",   # unused loop control
    "B019",   # cached_property on instance methods
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["N806"]

[tool.mypy]
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = [
    "cairo",
    "cairo.*",
    "cairosvg",
    "cairosvg.*",
    "discord",
    "discord.*",
    "click",
    "click.*",
    "rich",
    "rich.*",
]
ignore_missing_imports = true
